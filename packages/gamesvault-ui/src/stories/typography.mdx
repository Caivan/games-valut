import { Meta, Typeset } from '@storybook/addon-docs/blocks';
import { baseTokens } from '../styles/themes/baseTokens';

<Meta title="Game Vault Typography" />

# Typography - regular

<Typeset
  fontSizes={[
     32, // 2rem — large headings
      24, // 1.5rem — section headings
      20, // 1.25rem — subheadings
      16, // 1rem — main body text
      14, // 0.875rem — secondary text
      12, // 0.75rem — metadata, tags
  ]}
  fontWeight={baseTokens.typography.weights.regular}
  sampleText={'The best way to predict the future is to create it.'}
  fontFamily={baseTokens.typography.fontFamily}
/>

# Typography - medium

<Typeset
  fontSizes={[
     32, // 2rem — large headings
      24, // 1.5rem — section headings
      20, // 1.25rem — subheadings
      16, // 1rem — main body text
      14, // 0.875rem — secondary text
      12, // 0.75rem — metadata, tags
  ]}
  fontWeight={baseTokens.typography.weights.medium}
  sampleText={'The best way to predict the future is to create it.'}
  fontFamily={baseTokens.typography.fontFamily}

/>

# Typography - semibold

<Typeset
  fontSizes={[
     32, // 2rem — large headings
      24, // 1.5rem — section headings
      20, // 1.25rem — subheadings
      16, // 1rem — main body text
      14, // 0.875rem — secondary text
      12, // 0.75rem — metadata, tags
  ]}
  fontWeight={baseTokens.typography.weights.semibold}
  sampleText={'The best way to predict the future is to create it.'}
  fontFamily={baseTokens.typography.fontFamily}
/> 

# Typography - bold

<Typeset
  fontSizes={[
     32, // 2rem — large headings
      24, // 1.5rem — section headings
      20, // 1.25rem — subheadings
      16, // 1rem — main body text
      14, // 0.875rem — secondary text
      12, // 0.75rem — metadata, tags
  ]}
  fontWeight={baseTokens.typography.weights.bold}
  sampleText={'The best way to predict the future is to create it.'}
  fontFamily={baseTokens.typography.fontFamily}
/> 

{/* 
regular: 400,
      medium: 500,
      semibold: 600,
      bold: 700, */}